"""Test package for m3talex.

All binary fixtures are built programmatically in setUp methods via
:mod:`tests.m3talex.samplegen` — no opaque binary blobs are committed, and
every fixture's expected parse result is knowable by construction.
"""
